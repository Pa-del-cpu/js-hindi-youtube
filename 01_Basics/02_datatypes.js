"use strict"; // treat all js code as new version.

console.log(3+3) 

let age = 14 //number
let name = "Urvashi"  //string
let isLoggedIn = true //boolean
let countryName; //output = undefined //A variale that has been declared but not yet assigned any value. 
let temprature  = Null //Null represents the intentional absence of any object value.
//BigInt used for larger integers. Specially used for facebook, tarding.
// symbols = to indetufy uniqueness

console.log(typeof name);
console.log(typeof age);